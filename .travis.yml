language: node_js
os:
  - linux
  - osx
  - windows

node_js:
  - node
  - 14

jobs: 
  if: os = windows
  - YARN_GPG=no
  
cache: yarn

script:
  - ./packages/prismic-cli/bin/run --version
  - ./packages/prismic-cli/bin/run --help
  - yarn run test

