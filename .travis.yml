language: node_js
os:
  - linux
  - osx
  - windows

node_js:
  - node
  - 14

env: 
  - YARN_GPG=no # this is so windows correctly hangs up
  
cache: yarn

script:
  - ./packages/prismic-cli/bin/run --version
  - ./packages/prismic-cli/bin/run --help
  - yarn run test

